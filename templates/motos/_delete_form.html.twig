<form method="post" action="{{ path('app_motos_delete', {'id': moto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moto.id) }}">
    <button class="btn">Delete</button>
</form>
