<form method="post" action="{{ path('app_cmd_delete', {'id': cmd.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cmd.id) }}">
    <button class="btn">Delete</button>
</form>
