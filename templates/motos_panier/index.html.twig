{% extends 'base.html.twig' %}

{% block title %}MotosPanier index{% endblock %}

{% block body %}
    <h1>MotosPanier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quntity</th>
                <th>Total_price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for motos_panier in motos_paniers %}
            <tr>
                <td>{{ motos_panier.id }}</td>
                <td>{{ motos_panier.quntity }}</td>
                <td>{{ motos_panier.totalPrice }}</td>
                <td>
                    <a href="{{ path('app_motos_panier_show', {'id': motos_panier.id}) }}">show</a>
                    <a href="{{ path('app_motos_panier_edit', {'id': motos_panier.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_motos_panier_new') }}">Create new</a>
{% endblock %}
